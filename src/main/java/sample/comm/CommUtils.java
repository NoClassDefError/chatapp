package sample.comm;

import java.net.Socket;

public class CommUtils {
    /**
     * 用于发送内容
     * @return 发送是否成功
     */
    public boolean sendText(){
        Socket ss = new Socket();
        return true;
    }
}
